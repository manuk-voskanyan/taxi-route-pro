import {userType} from './user'
import {tripType} from './trip'
import {messageType} from './message'
import {ratingType} from './rating'

export const schema = {
  types: [userType, tripType, messageType, ratingType],
}
